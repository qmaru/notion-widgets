import Container from '@mui/material/Container'

export default function Template() {
  return (
    <Container
      disableGutters
      maxWidth={false}
      sx={{ padding: 0 }}
    >
      Template
    </Container>
  )
}